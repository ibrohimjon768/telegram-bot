services:
  - type: worker
    name: telegram-bot
    environment: python
    plan: free
    runtime: python
    branch: main
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python bot.py"
    autoDeploy: true
